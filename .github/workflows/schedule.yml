name: schedule Workflow

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  schedule-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Schedule Trigger
        run: echo "This Workflow runs every 5 muin"
