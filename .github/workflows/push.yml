name: push workflow

on:
  push:
      branches:
        - main


jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: Print Push Events
              run: echo "This Workflow is Triggered by a Push to the Main Branch"
